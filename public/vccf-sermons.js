(() => {
'use strict';
if(window.__VCCF_SERMONS__)return;window.__VCCF_SERMONS__=true;
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const client=()=>window.supabase?.createClient?.(window.VCCF_SUPABASE_URL,window.VCCF_SUPABASE_PUBLISHABLE_KEY);
let sb=null,user=null,profile=null,sermons=[];
const allowed=new Set(['application/pdf','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.ms-powerpoint','application/vnd.openxmlformats-officedocument.presentationml.presentation']);
const maxSize=50*1024*1024;
const css=`#vccf-sermons-view{display:none!important;padding:20px 0 40px}#vccf-sermons-view.active{display:block!important}.sermons-shell{background:var(--panel);border:1px solid var(--line);border-radius:20px;padding:20px;box-shadow:0 10px 30px rgba(16,24,40,.06)}.sermons-head{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-bottom:18px;flex-wrap:wrap}.sermons-head h2{margin:0}.sermons-head p{margin:4px 0 0;color:var(--muted)}.sermon-admin{border:1px solid var(--line);border-radius:16px;padding:16px;margin-bottom:18px;background:var(--bg)}.sermon-form{display:grid;grid-template-columns:1fr 1fr;gap:10px}.sermon-form .full{grid-column:1/-1}.sermon-form input,.sermon-form textarea{width:100%;box-sizing:border-box;border:1px solid var(--line);border-radius:11px;padding:11px;background:var(--panel);color:var(--text)}.sermon-form textarea{min-height:80px;resize:vertical}.sermon-form button{border:0;border-radius:11px;padding:11px 16px;background:var(--brand-gradient);color:#fff;font-weight:800;cursor:pointer}.sermon-list{display:grid;gap:10px}.sermon-card{display:flex;align-items:center;gap:14px;border:1px solid var(--line);border-radius:15px;padding:14px;background:var(--panel)}.sermon-icon{width:42px;height:42px;border-radius:12px;display:grid;place-items:center;background:var(--brand-gradient);color:#fff;font-weight:900;flex:none}.sermon-info{min-width:0;flex:1}.sermon-info strong{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.sermon-info small{display:block;color:var(--muted);margin-top:4px}.sermon-actions{display:flex;gap:7px;flex-wrap:wrap}.sermon-actions button{border:1px solid var(--line);background:var(--panel);color:var(--text);border-radius:10px;padding:8px 11px;cursor:pointer}.sermon-actions button.danger{color:var(--danger)}.sermon-empty{padding:35px;text-align:center;color:var(--muted);border:1px dashed var(--line);border-radius:15px}@media(max-width:700px){.sermon-form{grid-template-columns:1fr}.sermon-form .full{grid-column:auto}.sermon-card{align-items:flex-start;flex-wrap:wrap}.sermon-actions{width:100%}}`;
const addCss=()=>{if(document.getElementById('vccf-sermons-style'))return;const s=document.createElement('style');s.id='vccf-sermons-style';s.textContent=css;document.head.appendChild(s)};
const toast=msg=>{const t=document.getElementById('toast');if(t){t.textContent=msg;t.classList.add('show');clearTimeout(window.__vccfSermonToast);window.__vccfSermonToast=setTimeout(()=>t.classList.remove('show'),2800)}else alert(msg)};
function addNav(){const nav=document.querySelector('.nav');if(!nav)return;if(!nav.querySelector('[data-view="sermons"]')){const b=document.createElement('button');b.type='button';b.dataset.view='sermons';b.textContent='📖 Sermons';nav.appendChild(b)}}
function ensureView(){let v=document.getElementById('vccf-sermons-view');if(v)return v;v=document.createElement('section');v.id='vccf-sermons-view';v.className='view';v.innerHTML='<div class="sermons-shell"><div class="sermons-head"><div><h2>📖 Sermons</h2><p>Church sermon files and teaching materials.</p></div></div><div id="vccf-sermon-admin"></div><div id="vccf-sermon-list" class="sermon-list"><div class="sermon-empty">Loading sermons…</div></div></div>';const main=document.querySelector('.main');(main||document.querySelector('main')||document.body).appendChild(v);return v}
function hide(){const v=document.getElementById('vccf-sermons-view');if(v){v.classList.remove('active');v.style.setProperty('display','none','important')}}
function showView(){const v=ensureView();document.querySelectorAll('.nav [data-view]').forEach(x=>x.classList.toggle('active',x.dataset.view==='sermons'));document.querySelectorAll('.view').forEach(x=>{if(x!==v)x.classList.remove('active')});v.classList.add('active');v.style.setProperty('display','block','important');const title=document.getElementById('pageTitle');if(title)title.textContent='Sermons';load()}
const fmtSize=n=>{if(!n)return '';const u=['B','KB','MB','GB'];let i=0,x=n;while(x>=1024&&i<u.length-1){x/=1024;i++;}return `${x.toFixed(i?1:0)} ${u[i]}`};
const fmtDate=s=>{try{return new Date(s).toLocaleString(undefined,{dateStyle:'medium',timeStyle:'short'})}catch{return s}};
async function refreshSession(){const r=await sb.auth.getSession();user=r.data.session?.user||null;if(!user)return;const p=await sb.from('profiles').select('role,display_name').eq('user_id',user.id).maybeSingle();profile=p.data||null}
function renderAdmin(){const el=document.getElementById('vccf-sermon-admin');if(!el)return;if(profile?.role!=='admin'){el.innerHTML='';return}el.innerHTML='<div class="sermon-admin"><strong>Admin: Upload Sermon</strong><div style="color:var(--muted);font-size:.82rem;margin:5px 0 12px">Accepted: PDF, DOC, DOCX, PPT, PPTX • Maximum 50 MB</div><form id="vccf-sermon-form" class="sermon-form"><input name="title" placeholder="Sermon title" required><input name="file" type="file" accept=".pdf,.doc,.docx,.ppt,.pptx,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/vnd.ms-powerpoint,application/vnd.openxmlformats-officedocument.presentationml.presentation" required><textarea class="full" name="description" placeholder="Description (optional)"></textarea><button class="full" type="submit">Upload Sermon</button></form></div>';el.querySelector('form').addEventListener('submit',upload)}
async function load(){if(!sb)sb=client();if(!sb)return;try{await refreshSession();renderAdmin();const r=await sb.from('vccf_sermons').select('*').order('created_at',{ascending:false});if(r.error)throw r.error;sermons=r.data||[];renderList()}catch(e){console.error('[VCCF Sermons]',e);const el=document.getElementById('vccf-sermon-list');if(el)el.innerHTML='<div class="sermon-empty">Unable to load sermons. Please refresh and try again.</div>'}}
function renderList(){const el=document.getElementById('vccf-sermon-list');if(!el)return;if(!sermons.length){el.innerHTML='<div class="sermon-empty">No sermons have been uploaded yet.</div>';return}el.innerHTML=sermons.map(s=>`<article class="sermon-card"><div class="sermon-icon">${s.mime_type.includes('pdf')?'PDF':s.mime_type.includes('word')?'DOC':'PPT'}</div><div class="sermon-info"><strong>${esc(s.title)}</strong><small>${esc(s.file_name)} • ${fmtSize(s.file_size)} • ${fmtDate(s.created_at)}</small>${s.description?`<small>${esc(s.description)}</small>`:''}</div><div class="sermon-actions"><button data-open="${s.id}">Download</button>${profile?.role==='admin'?`<button class="danger" data-delete="${s.id}">Delete</button>`:''}</div></article>`).join('');el.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>downloadFile(b.dataset.open)));el.querySelectorAll('[data-delete]').forEach(b=>b.addEventListener('click',()=>removeSermon(b.dataset.delete)))}
async function upload(e){e.preventDefault();const form=e.currentTarget,file=form.file.files[0],title=form.title.value.trim(),description=form.description.value.trim();if(!file||!title)return;if(!allowed.has(file.type)){toast('Unsupported file format. Use PDF, DOC, DOCX, PPT, or PPTX.');return}if(file.size>maxSize){toast('File is larger than 50 MB.');return}const btn=form.querySelector('button');btn.disabled=true;btn.textContent='Uploading…';try{const safe=file.name.replace(/[^a-zA-Z0-9._-]+/g,'_'),path=`${user.id}/${crypto.randomUUID()}-${safe}`;const up=await sb.storage.from('vccf-sermons').upload(path,file,{contentType:file.type,upsert:false});if(up.error)throw up.error;const ins=await sb.from('vccf_sermons').insert({title,description:description||null,file_path:path,file_name:file.name,mime_type:file.type,file_size:file.size,uploaded_by:user.id});if(ins.error){await sb.storage.from('vccf-sermons').remove([path]);throw ins.error}form.reset();toast('Sermon uploaded successfully.');await load()}catch(err){console.error('[VCCF Sermons upload]',err);toast(err.message||'Upload failed')}finally{btn.disabled=false;btn.textContent='Upload Sermon'}}
async function downloadFile(id){const s=sermons.find(x=>x.id===id);if(!s)return;const r=await sb.storage.from('vccf-sermons').createSignedUrl(s.file_path,600,{download:s.file_name});if(r.error){toast(r.error.message||'Unable to download file.');return}const a=document.createElement('a');a.href=r.data.signedUrl;a.download=s.file_name;a.target='_blank';a.rel='noopener';document.body.appendChild(a);a.click();a.remove()}
async function removeSermon(id){const s=sermons.find(x=>x.id===id);if(!s||profile?.role!=='admin')return;if(!confirm(`Delete “${s.title}”?`))return;const r=await sb.storage.from('vccf-sermons').remove([s.file_path]);if(r.error){toast(r.error.message||'Unable to delete file.');return}const d=await sb.from('vccf_sermons').delete().eq('id',id);if(d.error){toast(d.error.message||'Unable to delete sermon record.');return}toast('Sermon deleted.');await load()}
function boot(){addCss();addNav();ensureView();hide();setTimeout(()=>{addNav();ensureView();hide()},1000)}boot();document.addEventListener('click',e=>{const b=e.target.closest('.nav [data-view]');if(!b)return;if(b.dataset.view==='sermons')showView();else hide()});window.VCCFSermons={open:showView,refresh:load,getSermon:id=>sermons.find(x=>x.id===id)};
})();