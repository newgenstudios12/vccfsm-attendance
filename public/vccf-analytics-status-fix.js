(()=>{
'use strict';
if(window.__VCCF_ANALYTICS_STATUS_FIX_V6__) return;
window.__VCCF_ANALYTICS_STATUS_FIX_V6__=true;
const getClient=()=>window.__VCCF_SHARED_SUPABASE_CLIENT__||window.supabase?.createClient?.(window.VCCF_SUPABASE_URL,window.VCCF_SUPABASE_PUBLISHABLE_KEY);
const roleName=r=>String(r||'').trim().toLowerCase().replace(/_/g,' ');
const statusOf=m=>String(m?.status||'').trim().toLowerCase()==='inactive'||m?.is_active===false?'inactive':'active';
async function refreshStats(){try{const app=document.getElementById('app');if(!app?.classList.contains('active'))return;const c=getClient();if(!c)return;const {data:{user},error:ue}=await c.auth.getUser();if(ue||!user)return;const {data:profile,error:pe}=await c.from('profiles').select('role,area_id').eq('user_id',user.id).maybeSingle();if(pe||!profile)return;const role=roleName(profile.role);if(role!=='admin'&&role!=='area leader')return;let q=c.from('members').select('id,area_id,status,is_active');if(role==='area leader')q=q.eq('area_id',profile.area_id);const {data:members,error:me}=await q;if(me)return;let scoped=Array.isArray(members)?members:[];const filter=document.getElementById('memberStatsArea')||document.querySelector('#memberStatsFilter select');if(role==='admin'&&filter?.value&&!/^(all|total)$/i.test(String(filter.value)))scoped=scoped.filter(m=>String(m.area_id)===String(filter.value));const total=scoped.length,inactive=scoped.filter(m=>statusOf(m)==='inactive').length,active=total-inactive;document.querySelectorAll('.suite-kpi').forEach(card=>{const label=(card.querySelector('small')?.textContent||'').trim().toLowerCase();const value=card.querySelector('strong');if(!value)return;if(label==='active')value.textContent=active;else if(label==='inactive')value.textContent=inactive;else if(label.includes('total')&&label.includes('member'))value.textContent=total});}catch(e){console.warn('VCCF analytics statistics:',e)}}
function schedule(delay=500){clearTimeout(window.__VCCF_ANALYTICS_STATUS_TIMER__);window.__VCCF_ANALYTICS_STATUS_TIMER__=setTimeout(refreshStats,delay)}
function boot(){if(!document.getElementById('app')?.classList.contains('active'))return;if(window.__VCCF_ANALYTICS_STATUS_LISTENERS__)return;window.__VCCF_ANALYTICS_STATUS_LISTENERS__=true;document.addEventListener('click',e=>{if(e.target.closest?.('button[data-view="analytics"],button[data-view="dashboard"],button[data-view="members"],#memberStatsArea,#memberStatsFilter select'))schedule(450)});document.addEventListener('change',e=>{if(e.target.closest?.('#memberStatsArea,#memberStatsFilter select,.vccf-inline-status'))schedule(600)});schedule(500)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.addEventListener('vccf-authenticated',boot);
})();
