(()=>{'use strict';if(window.__VCCF_STABILITY_GUARD_V1__)return;window.__VCCF_STABILITY_GUARD_V1__=true;let pending=false;function run(){pending=false;const nav=document.querySelector('.nav');if(nav){const p=[...nav.querySelectorAll('button')].filter(b=>String(b.dataset.view||'').toLowerCase()==='myprofile'||/^(my\s*profile|profile)$/i.test((b.textContent||'').replace(/\s+/g,' ').trim()));p.slice(1).forEach(b=>b.remove())}}function schedule(){if(pending)return;pending=true;requestAnimationFrame(run)}window.addEventListener('vccf-authenticated',schedule);window.addEventListener('vccf-profile-linked',schedule);document.addEventListener('click',e=>{if(e.target.closest('.nav button'))setTimeout(schedule,0)});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',schedule,{once:true});else schedule()})();
